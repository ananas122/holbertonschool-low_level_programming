#include <stdio.h>
#include <time.h>

/*
 * main - Entry point 
 *Return Alawyas 0 (success)
 */
int main(void)

{ int n;

  srand(time(0))
    n = rand () -RAND_MAX / 2;

  if (n % 5 > 10)
    printf("last digit of %d is %d and is greater than 5\n")

    else (n == 


	  

  return (0) ;

}
