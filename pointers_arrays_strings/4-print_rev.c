#include <stdio.h>
/**
 * print_rev - Prints a string in reverse
 * @s: The string to print
 * Return: void
 */
void print_rev(char *s);
{
	int c = 0;

	if (*s != '\0')

	for (c -= 1; c >= 0; c--)
	{
		_putchar(str[c]);
	}

	_putchar('\n');
}

