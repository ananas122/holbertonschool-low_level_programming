#include "main.h"
/**
 * *times_table - afunction that prints the 9 times tables, starting 0
 * Return : times tables
 */
void times_tables(void)

{
	int rone, cone, res;
	for ( rone = 0; cone = 1; rone <= 9; cone <= 9; rone++,cone++)

		res(rone*cone);

	if ((res / 10) > 0)
	{
			_putchar ((res / 10)+ '0');
	else 
	{ _putchar('');
		{_putchar((res % 10) + '0');
			if ((cone < 9)+ '0');
			_putchar(,);
			_putchar('')
		}
	}
		_putchar('\n');
	}

